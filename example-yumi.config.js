module.exports = {
  /**
   * El token del bot. Para obtener un token, se debe crear
   * una aplicación en discord y asignarle un usuario *bot* para esto:
   *  - ir a <https://discordapp.com/developers/applications/me>
   *  - crear nueva aplicación
   *  - ir a la sección `Bot`
   *  - asignarle un usuario *bot* (`Add Bot`)
   *  - revelar token
   * finalmente copiar y pegar en este apartado el token revelado.
   *
   * Nota: el token expuesto aquí no funciona, es solo de ejemplo, en caso de
   * usarse, la aplicación lanzará una excepción indicando que datos erróneos
   * fueron provistos.
   */
  token: 'NDY0ODgzOTQzNTgyMjAzOTM1.Dj53dg.yZ53VVQ58WyUFI2LiBXpolHDUYY',
  /**
   * Prefijo del bot. Si no se especifica alguno se utilizará el prefijo
   * yu!
   */
  prefix: '<<'
}
